# deals one card
class dealcard:
	
	def __init_(self,shuffled_cards):
		self.shuffled_cards = shuffled_cards
		self.face = ''
		self.suit = ''

	def get_deal_card():
		dealt_card = self.shuffled_cards.pop(0)
		self.suit = dealt.card[0]
		self.face = dealt.card[1]
		
		return card_val
